---
tags:
  - dp
  - 状态机模型
  - easy
date: 2026-05-13
练习次数: 1
---
# 题目

[1049. 大盗阿福 - AcWing题库](https://www.acwing.com/problem/content/1051/)

阿福是一名经验丰富的大盗。趁着月黑风高，阿福打算今晚洗劫一条街上的店铺。

这条街上一共有 N 家店铺，每家店中都有一些现金。

阿福事先调查得知，只有当他同时洗劫了两家相邻的店铺时，街上的报警系统才会启动，然后警察就会蜂拥而至。

作为一向谨慎作案的大盗，阿福不愿意冒着被警察追捕的风险行窃。

他想知道，在不惊动警察的情况下，他今晚最多可以得到多少现金？

#### 输入格式

输入的第一行是一个整数 T，表示一共有 T 组数据。

接下来的每组数据，第一行是一个整数 N ，表示一共有 N 家店铺。

第二行是 N 个被空格分开的正整数，表示每一家店铺中的现金数量。

每家店铺中的现金数量均不超过1000。

#### 输出格式

对于每组数据，输出一行。

该行包含一个整数，表示阿福在不惊动警察的情况下可以得到的现金数量。

#### 数据范围

1≤T≤50
1≤N≤$10^5$

#### 输入样例：

```
2
3
1 8 2
4
10 7 6 14
```

#### 输出样例：

```
8
24
```

#### 样例解释

对于第一组样例，阿福选择第2家店铺行窃，获得的现金数量为8。

对于第二组样例，阿福选择第1和4家店铺行窃，获得的现金数量为10+14=24。

# 思路

这道题有点像数字三角形模型，先复习一下[[AcWing 898. 数字三角形]]

![[Pasted image 20260513203520.png]]

![[Pasted image 20260513203541.png]]

# 代码

```c++
#include<bits/stdc++.h> // 包含所有常用标准库
using namespace std;

const int N = 1e5 + 10; // 定义数组最大长度
int n, T;
int w[N];               // 存储每个物品的价值/权重
int f[N][2];            // DP状态数组：f[i][0] 表示不选第 i 个物品，f[i][1] 表示选第 i 个物品

int main() {
    cin >> T;           // 输入测试用例的数量
    while (T--) {
        cin >> n;       // 输入当前用例的物品数量
        for (int i = 1; i <= n; i++) cin >> w[i]; // 输入每个物品的具体价值

        // 初始化：第 0 个物品选或不选的价值都是 0
        f[0][0] = f[0][1] = 0;

        // 核心动态规划过程
        for (int i = 1; i <= n; i++) {
            // 情况 1：选择第 i 个物品
            // 前提是第 i-1 个物品必须【没有被选】，所以加上 f[i-1][0]
            f[i][1] = f[i - 1][0] + w[i];

            // 情况 2：不选第 i 个物品
            // 那么第 i-1 个物品【选或不选】都可以，取其中的最大值
            f[i][0] = max(f[i - 1][1], f[i - 1][0]);
        }

        // 输出结果：在处理完最后一个物品后，选或不选两种状态中的最大值
        cout << max(f[n][0], f[n][1]) << endl;
    }
}
```