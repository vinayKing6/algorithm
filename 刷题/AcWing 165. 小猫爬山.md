---
tags:
  - graph
  - dfs
  - 剪枝
  - mid
date: 2026-03-21
练习次数: 3
---
# 题目

https://www.acwing.com/problem/content/167/

翰翰和达达饲养了 N 只小猫，这天，小猫们要去爬山。

经历了千辛万苦，小猫们终于爬上了山顶，但是疲倦的它们再也不想徒步走下山了（呜咕>_<）。

翰翰和达达只好花钱让它们坐索道下山。

索道上的缆车最大承重量为 W，而 N 只小猫的重量分别是 C1、C2……CN。

当然，每辆缆车上的小猫的重量之和不能超过 W。

每租用一辆缆车，翰翰和达达就要付 1 美元，所以他们想知道，最少需要付多少美元才能把这 N 只小猫都运送下山？

#### 输入格式

第 1 行：包含两个用空格隔开的整数，N 和 W。

第 2..N+1 行：每行一个整数，其中第 i+1 行的整数表示第 i 只小猫的重量 $C_i$。

#### 输出格式

输出一个整数，表示最少需要多少美元，也就是最少需要多少辆缆车。

#### 数据范围

1≤N≤18,  
1≤$C_i$≤W≤$10^8$

#### 输入样例：

```
5 1996
1
2
1994
12
29
```

#### 输出样例：

```
2
```

# 思路

![[Pasted image 20260305144408.png]]
![[Pasted image 20260305144417.png]]
# 代码

```c++
#include<bits/stdc++.h>
using namespace std;

const int N = 20;
int n, m;          // n: 小猫数量, m: 缆车最大承重
int w[N];          // 每只小猫的重量
int sum[N];        // 存储当前每辆缆车已经装载的总重量
int ans;           // 存储最终答案（最少缆车数）

/**
 * DFS 搜索函数
 * @param c 当前正在安排第几只小猫（从 0 开始）
 * @param k 当前已经使用了多少辆缆车
 */
void dfs(int c, int k) {
    // 【剪枝 1：最优性剪枝】
    // 如果当前开辟的缆车数量已经大于或等于目前搜到的最好结果，直接回溯
    if (k >= ans)
        return;

    // 【终止条件】
    // 如果所有的小猫都已经安排妥当
    if (c == n) {
        ans = k; // 更新全局最优解
        return;
    }
    
    // --- 情况 1：尝试将当前小猫放入已经开辟的缆车中 ---
    for (int i = 0; i < k; i++) {
        // 判断当前第 i 辆缆车是否还能装下这只小猫
        if (sum[i] + w[c] <= m) {
            sum[i] += w[c];    // 放入
            dfs(c + 1, k);     // 递归处理下一只猫
            sum[i] -= w[c];    // 【回溯】取出，尝试其他方案
        }
    }
    
    // --- 情况 2：尝试为当前小猫新开一辆缆车 ---
    // sum[k] 代表当前空着的第 k 辆缆车（下标从 0 到 k-1 已经有猫了）
    sum[k] = w[c];
    dfs(c + 1, k + 1);         // 递归处理，缆车数量 k 加 1
    sum[k] -= w[c];            // 【回溯】
}

int main() {
    // 读入猫的数量和缆车载重
    cin >> n >> m;
    ans = n; // 初始化答案为最大可能值（一只猫占一辆车）

    for (int i = 0; i < n; i++) cin >> w[i];
    
    // 【剪枝 2：优化搜索顺序】
    // 将猫按重量从大到小排序。
    // 这是一个非常关键的优化：先处理重猫，可以更快地填满缆车，
    // 从而更容易触发 k >= ans 的剪枝条件，大幅缩短搜索时间。
    sort(w, w + n, greater<int>());
    
    // 从第 0 只猫开始搜索，此时已开辟 0 辆缆车
    dfs(0, 0);
    
    // 输出最终结果
    cout << ans << endl;
    
    return 0;
}
```