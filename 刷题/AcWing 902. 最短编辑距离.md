---
tags:
  - dp
  - 线性dp
  - mid
date: 2026-02-07
练习次数: 1
---
# 题目

https://www.acwing.com/problem/content/904/

给定两个字符串 $A$ 和 $B$，现在要将 $A$ 经过若干操作变为 $B$。可进行的操作有：

- **删除**：将字符串 $A$ 中的某个字符删除；
- **插入**：在字符串 $A$ 的某个位置插入某个字符；
- **替换**：将字符串 $A$ 中的某个字符替换为另一个字符。

求出将 $A$ 变为 $B$ 至少需要进行多少次操作。

输入格式

- 第一行包含整数 $n$，表示字符串 $A$ 的长度。
- 第二行包含一个长度为 $n$ 的字符串 $A$。
- 第三行包含整数 $m$，表示字符串 $B$ 的长度。
- 第四行包含一个长度为 $m$ 的字符串 $B$。

字符串中均只包含大小写字母。

- 数据范围：$1 \le n, m \le 1000$

输出格式

输出一个整数，表示最少操作次数。

输入样例

```
10
AGTCTGACGC
11
AGTAAGTAGGC
```

输出样例

```
4
```

# 思路

https://www.acwing.com/solution/content/5607/

![[Pasted image 20260207152248.png]]

集合：将$a[1~i]$变成$b[1~j]$的操作方式
属性：min

有三种操作，所以有三个子集 
ok子集划分完了 
考虑状态转移的时候 
先考虑如果我没有进行这个操作应该是什么状态 
然后考虑你进行这一步操作之后会对你下一个状态造成什么影响 
然后再加上之前状态表示中你决策出来的那个DP属性 
这样就可以自然而然地搞出来转移方程啦 

1)删除操作：把$a[i]$删掉之后$a[1\sim~i]$和$b[1\sim~j]$匹配 所以之前要先做到$a[1 \sim (i-1)]$和$b[1\sim~j]$匹配 $f[i-1][j] + 1$

2)插入操作：插入之后$a[i]$与$b[j]$完全匹配，所以插入的就是$b[j]$ 那填之前$a[1\sim~i]$和$b[1\sim (j-1)]$匹配 $f[i][j-1] + 1$

3)替换操作：把$a[i]$改成$b[j]$之后想要$a[1\sim i]$与$b[1\sim j]$匹配 那么修改这一位之前，$a[1\sim (i-1)]$应该与$b[1\sim (j-1)]$匹配 $f[i-1][j-1] + 1$

但是如果本来$a[i]$与$b[j]$这一位上就相等，那么不用改，即 $f[i-1][j-1] + 0$ 
好的那么$f[i][j]$就由以上三个可能状态转移过来，取个min

即 $f[i][j]=\mathrm{min}(f[i][j],f[i-1][j]+1,f[i][j-1]+1,f[i-1][j-1]+1)$,并且所有字符串都可以通过删除 插入 修改操作转换到$f[i][j]$,所以取最小值

# 代码

```c++
#include<bits/stdc++.h>  // 包含所有标准库头文件（竞赛常用，实际开发中不推荐）
using namespace std;    // 使用标准命名空间

const int N=1010;       // 定义最大数组尺寸为1010，足够处理1000以内的字符串长度

int n,m;                // n: 字符串a的长度，m: 字符串b的长度
int dp[N][N];           // DP数组：dp[i][j]表示将a的前i个字符变为b的前j个字符的最小编辑距离
char a[N],b[N];         // 字符数组存储两个字符串，索引从1开始使用

int main(){
    // 输入：n个字符的字符串a，m个字符的字符串b
    // a+1表示从数组的第1个位置（下标1）开始存放字符串
    cin>>n>>a+1>>m>>b+1;

    // 初始化DP边界条件：
    // 将空字符串变为b的前j个字符需要j次插入操作
    for(int i=0;i<=m;i++) dp[i]=i;
    // 将a的前i个字符变为空字符串需要i次删除操作
    for(int i=0;i<=n;i++) dp[i][0]=i;

    // 动态规划计算最小编辑距离
    for(int i=1;i<=n;i++){         // 遍历字符串a的每个字符
        for(int j=1;j<=m;j++){     // 遍历字符串b的每个字符
            if(a[i] == b[j])
                // 当前字符相同：无需额外操作，继承dp[i-1][j-1]
                dp[i][j]=dp[i-1][j-1];
            else{
                // 当前字符不同：考虑三种编辑操作的最小值
                // 1) 删除a[i]   -> dp[i-1][j] + 1
                // 2) 插入b[j]   -> dp[i][j-1] + 1
                dp[i][j]=min(dp[i-1][j]+1,dp[i][j-1]+1);
                // 3) 将a[i]替换为b[j] -> dp[i-1][j-1] + 1
                dp[i][j]=min(dp[i][j],dp[i-1][j-1]+1);
            }
        }
    }

    // 输出结果：将a的前n个字符变为b的前m个字符的最小编辑距离
    cout<<dp[n][m]<<endl;
    
    return 0;
}
```

