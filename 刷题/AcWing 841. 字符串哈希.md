---
tags:
  - 查找
  - 字符串
  - easy
date: 2026-03-07
练习次数: 1
---
# 题目

给定一个长度为 n 的字符串，再给定 m 个询问，每个询问包含四个整数 l1,r1,l2,r2，请你判断 `[l1,r1]` 和`[l2,r2]` 这两个区间所包含的字符串子串是否完全相同。

字符串中只包含大小写英文字母和数字。

#### 输入格式

第一行包含整数 n 和 m，表示字符串长度和询问次数。

第二行包含一个长度为 n 的字符串，字符串中只包含大小写英文字母和数字。

接下来 m 行，每行包含四个整数 l1,r1,l2,r2，表示一次询问所涉及的两个区间。

注意，字符串的位置从 1 开始编号。

#### 输出格式

对于每个询问输出一个结果，如果两个字符串子串完全相同则输出 `Yes`，否则输出 `No`。

每个结果占一行。

#### 数据范围

1≤n,m≤$10^5$

#### 输入样例：

```
8 3
aabbaabb
1 3 5 7
1 3 6 8
1 2 1 2
```

#### 输出样例：

```
Yes
No
Yes
```

# 思路
![[Pasted image 20260307122523.png]]

![[Pasted image 20260307105352.png]]

# 代码

```c++
#include <bits/stdc++.h>

using namespace std;

// N 为字符串最大长度，P 为进制数（经验值 131 或 13331，能有效降低碰撞概率）
const int N = 1e5 + 10;
const int P = 131;

// 使用 unsigned long long (ULL) 存储哈希值
// ULL 的特性是溢出后自动对 2^64 取模，相当于免去了手动 % M 的操作
typedef unsigned long long ULL;

// h[i] 存储字符串前 i 个字符构成的哈希值（前缀哈希）
// p[i] 存储 P 的 i 次方，用于计算区间哈希时对齐位权
ULL h[N], p[N];
int n, m;
string s;

/**
 * get 函数用于 O(1) 获取子串 [l, r] 的哈希值
 * 原理：通过前缀和思想，减去高位部分的贡献
 * 注意：此处的 l 和 r 是从 1 开始计数的逻辑下标
 */
ULL get(int l, int r) {
    // 公式：子串哈希 = 右端点前缀哈希 - 左端点前缀哈希 * P^(子串长度)
    return h[r] - h[l - 1] * p[r - l + 1];
}

int main() {
    // 提高读入效率（如果数据量大，建议使用 scanf 或 cin.tie(0)）
    ios::sync_with_stdio(false);
    cin.tie(0);

    cin >> n >> m >> s;

    // 1. 预处理 P 的幂次数组和前缀哈希数组
    p[0] = 1; // P^0 = 1
    for (int i = 1; i <= n; i++) {
        p[i] = p[i - 1] * P;                     // 预计算 P^i
        h[i] = h[i - 1] * P + s[i - 1];          // 计算前缀哈希，s[i-1] 是因为 string 下标从 0 开始
    }

    // 2. 处理 m 次询问
    while (m--) {
        int l1, r1, l2, r2;
        cin >> l1 >> r1 >> l2 >> r2;

        // 3. 比较两个区间的哈希值，如果相等则认为子串相同
        if (get(l1, r1) == get(l2, r2)) {
            cout << "Yes" << endl;
        } else {
            cout << "No" << endl;
        }
    }

    return 0;
}
```