---
tags:
  - 队列
  - 单调队列
  - hard
date: 2026-07-20
练习次数: 2
---
# 题目

https://www.acwing.com/problem/content/156/

给定一个大小为 n≤$10^6$ 的数组。

有一个大小为 k 的滑动窗口，它从数组的最左边移动到最右边。

你只能在窗口中看到 k 个数字。

每次滑动窗口向右移动一个位置。

以下是一个例子：

该数组为 `[1 3 -1 -3 5 3 6 7]`，k 为 3。

|窗口位置|最小值|最大值|
|:-:|:-:|:-:|
|[1 3 -1] -3 5 3 6 7|-1|3|
|1 [3 -1 -3] 5 3 6 7|-3|3|
|1 3 [-1 -3 5] 3 6 7|-3|5|
|1 3 -1 [-3 5 3] 6 7|-3|5|
|1 3 -1 -3 [5 3 6] 7|3|6|
|1 3 -1 -3 5 [3 6 7]|3|7|

你的任务是确定滑动窗口位于每个位置时，窗口中的最大值和最小值。

#### 输入格式

输入包含两行。

第一行包含两个整数 n 和 k，分别代表数组长度和滑动窗口的长度。

第二行有 n 个整数，代表数组的具体数值。

同行数据之间用空格隔开。

#### 输出格式

输出包含两个。

第一行输出，从左至右，每个位置滑动窗口中的最小值。

第二行输出，从左至右，每个位置滑动窗口中的最大值。

#### 数据范围

1≤n≤106,  
1≤k≤n

#### 输入样例：

```
8 3
1 3 -1 -3 5 3 6 7
```

#### 输出样例：

```
-1 -3 -3 -3 3 3
3 3 5 5 6 7
```

# 思路

题目中提到了滑动窗口，我们先以示例为例看下什么是滑动窗口？在示例中，我们从数组中第一个元素开始遍历，由于窗口的大小是`3`，因此当遍历到第三个元素时，窗口就形成了。

![[Pasted image 20260309203919.png]]{:center}

之后，继续遍历元素时，为了保持窗口的大小为`3`，左侧元素就需要从窗口中剔除。这样使得窗口一直在向右移动，直到考察到最后一个元素结束，这就是所谓的滑动窗口。

![[Pasted image 20260309203932.png]]

解题思路（以最大值为例）：

由于我们需要求出的是滑动窗口的最大值。

- 如果当前的滑动窗口中有两个下标 `i` 和 `j` ，其中`i`在`j`的左侧（`i`<`j`），并且`i`对应的元素不大于j对应的元素（`nums[i]≤nums[j]`），则：
    
    当滑动窗口向右移动时，只要 `i` 还在窗口中，那么 `j` 一定也还在窗口中。这是由于 `i` 在 `j` 的左侧所保证的。
    
    **因此，由于 `nums[j]` 的存在，`nums[i]` 一定不会是滑动窗口中的最大值了，我们可以将`nums[i]`永久地移除。**
    
- 因此我们可以使用一个队列存储所有还没有被移除的下标。在队列中，这些下标按照从小到大的顺序被存储，并且它们在数组`nums`中对应的值是严格单调递减的。
    
- 当滑动窗口向右移动时，我们需要把一个新的元素放入队列中。
    
    为了保持队列的性质，我们会不断地将新的元素与队尾的元素相比较，如果新元素大于等于队尾元素，那么队尾的元素就可以被永久地移除，我们将其弹出队列。我们需要不断地进行此项操作，直到队列为空或者新的元素小于队尾的元素。
    
- 由于队列中下标对应的元素是严格单调递减的，因此此时队首下标对应的元素就是滑动窗口中的最大值。
    
- 窗口向右移动的时候。因此我们还需要不断从队首弹出元素保证队列中的所有元素都是窗口中的，因此当队头元素在窗口的左边的时候，弹出队头。
## 模拟

我给你画一个 **滑动窗口 + 单调队列全过程图**。  
数组例子用经典数据：

```
a = [1, 3, -1, -3, 5, 3, 6, 7]
k = 3
```

目标：求 **每个窗口的最小值**（维护单调递增队列）。

---

### ① 初始状态

数组：

```
index: 1  2  3  4  5  6  7  8
value: 1  3 -1 -3  5  3  6  7
```

窗口大小：

```
k = 3
```

队列（空）：

```
deque = []
```

---

### ② i = 1

新元素：

```
1
```

窗口：

```
[1]
```

队列维护递增：

```
deque = [1]
```

窗口未形成（<3）

---

### ③ i = 2

新元素：

```
3
```

比较队尾：

```
3 < 1 ? 否
```

入队：

```
deque = [1,3]
```

窗口：

```
[1,3]
```

仍未形成。

---

### ④ i = 3

新元素：

```
-1
```

维护单调：

```
3 > -1 → pop
1 > -1 → pop
```

队列：

```
deque = [-1]
```

窗口：

```
[1,3,-1]
```

图示：

```
[1  3  -1]
        ↑
      最小值
```

输出：

```
-1
```

---

### ⑤ i = 4

新元素：

```
-3
```

维护单调：

```
-1 > -3 → pop
```

队列：

```
deque = [-3]
```

窗口移动：

```
1 [3 -1 -3]
```

图示：

```
[3  -1  -3]
         ↑
       最小值
```

输出：

```
-3
```

---

### ⑥ i = 5

新元素：

```
5
```

比较：

```
5 > -3
```

入队：

```
deque = [-3,5]
```

窗口：

```
3 [-1 -3 5]
```

图示：

```
[-1  -3   5]
      ↑
    最小值
```

输出：

```
-3
```

---

### ⑦ i = 6

新元素：

```
3
```

维护单调：

```
5 > 3 → pop
```

队列：

```
deque = [-3,3]
```

窗口：

```
-1 [-3 5 3]
```

图：

```
[-3  5  3]
 ↑
最小值
```

输出：

```
-3
```

---

### ⑧ i = 7

新元素：

```
6
```

队列：

```
deque = [-3,3,6]
```

但窗口左端滑出：

```
a[4] = -3
```

队头正好是：

```
-3
```

删除：

```
deque = [3,6]
```

窗口：

```
-3 [5 3 6]
```

图：

```
[5  3  6]
   ↑
 最小值
```

输出：

```
3
```

---

### ⑨ i = 8

新元素：

```
7
```

队列：

```
deque = [3,6,7]
```

窗口：

```
5 [3 6 7]
```

图：

```
[3  6  7]
 ↑
最小值
```

输出：

```
3
```

---

### 最终最小值

```
-1 -3 -3 -3 3 3
```

---

### 单调队列结构直观图

队列始终保持：

```
递增
```

例如某时刻：

```
deque

front → [-3 , 3 , 6]
           ↑
        越往后越大
```

因此：

```
队头 = 当前窗口最小值
```

---

### 为什么能 O(n)

每个元素：

```
最多入队一次
最多出队一次
```

所以总操作：

```
≤ 2n
```

时间复杂度：

```
O(n)
```

---
# 代码

```c++
#include<bits/stdc++.h>
using namespace std;

const int N = 1e6 + 10;

int a[N];     // 存储输入数组
int n, k;     // n为数组长度，k为窗口大小
deque<int> q; // 双端队列，用来维护单调队列

int main(){
    cin >> n >> k;

    // 读入数组
    for(int i = 1; i <= n; i++){
        cin >> a[i];
    }

    // ----------- 求每个窗口的最小值 -----------

    for(int i = 1; i <= n; i++){

        // 维护单调递增队列
        // 如果新元素比队尾小，说明队尾不可能成为未来窗口最小值
        // 因此弹出队尾
        while(q.size() && q.back() > a[i])
            q.pop_back();

        // 当前元素入队
        q.push_back(a[i]);

        // 判断队头元素是否滑出窗口
        // 窗口左端是 i-k
        if(i >= k + 1 && a[i - k] == q.front())
            q.pop_front();

        // 当窗口形成（i>=k）时，输出最小值
        // 因为队列递增，所以队头就是最小值
        if(i >= k)
            cout << q.front() << " ";
    }

    cout << endl;

    // 清空队列，重新计算最大值
    q.clear();

    // ----------- 求每个窗口的最大值 -----------

    for(int i = 1; i <= n; i++){

        // 维护单调递减队列
        // 如果新元素比队尾大，则队尾不可能成为未来窗口最大值
        while(q.size() && q.back() < a[i])
            q.pop_back();

        // 当前元素入队
        q.push_back(a[i]);

        // 判断队头是否滑出窗口
        if(i - k >= 1 && a[i - k] == q.front())
            q.pop_front();

        // 当窗口形成，输出最大值
        // 因为队列递减，所以队头是最大值
        if(i >= k)
            cout << q.front() << " ";
    }
}
```